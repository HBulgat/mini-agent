# mini-agent 开发进度

> 更新机制：每完成一轮开发或一项任务，必须更新本文件
> 状态值：⏳ 未开始 / 🔄 进行中 / ✅ 已完成 / ⛔ 阻塞 / ❌ 取消

---

## 一、整体进度仪表盘

| 维度 | 状态 | 说明 |
|---|---|---|
| 需求文档 | ✅ 已锁定 | 含 Skill 模块补丁；详见 `../requirements/` |
| 系统设计 | 🔄 进行中 | R1–R6 + R7-1' 已锁定；R7-2/R8–R14 待开展 |
| 开发计划 | ✅ 基础版已制定 | Iter-0 / Iter-1 / Iter-2 全部前置依赖就绪 |
| 实际编码 | 🔄 进行中（Iter-2）| Iter-0 已完成 6/7；Iter-1 已完成 7/9；Iter-2 已完成 7/9 + T2.3 扩展，**P0 工具集 9 + agent.Loop v1 + AGENTS.md + 端到端冒烟 + 四模式验收全通** |

| 阶段 | 已完成任务 | 进行中 | 待开始 | 阻塞 |
|---|---|---|---|---|
| Iter-0（工程骨架） | 6 / 7 | 0 | 1 | 0 |
| Iter-1（LLM + 基础工具 + 会话） | 7 / 9 | 0 | 1 | 1 |
| Iter-2（Agent Loop + 完整工具集 + 权限） | 7 / 9 | 0 | 2 | 0 |
| Iter-3（压缩 + write_plan + task） | 0 / 5 | 0 | 5 | 0 |
| Iter-4（Skill + Trace + 多 Provider + 白黑名单） | 0 / 10 | 0 | 10 | 0 |
| Iter-5（Web UI 后端 + 前端骨架） | 0 / 6 | 0 | 6 | 0 |
| Iter-6（Web UI 完善 + 验收） | 0 / 7 | 0 | 7 | 0 |
| Iter-7（P3） | 0 / 4 | 0 | 4 | 0 |
| **总计** | **20 / 57** + 3 子任务 | **0** | **35** | **1** |

---

## 二、系统设计轮次进度

| 轮次 | 主题 | 状态 | 锁定时间 | 输出文件 |
|---|---|---|---|---|
| 元决策 | 文档粒度 + 技术栈 | ✅ 已锁定 | 2026-05-23 | `00-meta-decisions.md` |
| R1 | 总体架构 + 关键决定 + UIO + 工具集 + 权限矩阵 | ✅ 已锁定 | 2026-05-23 | `01-overall-architecture.md` / `02-key-decisions.md` / `03-uio-abstraction.md` / `04-tool-catalog.md` |
| R2 | 核心抽象与领域模型（接口签名） | ✅ 已锁定 | 2026-05-24 | `05-core-abstractions.md` |
| R3 | SQLite schema + Codec + 三层数据结构 + 思考模式 + 可见性 | ✅ 已锁定 | 2026-05-24 | `06-session-storage.md`（含对 05 的修订） |
| R4 | 配置 / AGENTS.md / 白黑名单 / Go 指针约定（D31） | ✅ 已锁定 | 2026-05-24 | `07-config-and-rules.md` |
| R5 | LLM Provider 适配 | ✅ 已锁定 | 2026-05-24 | `08-llm-providers.md`（含对 05/06/07 的就地修订）|
| R6 | Agent 执行引擎细化 | ✅ 已锁定 | 2026-05-24 | `09-agent-engine.md`（含对 05 §5.9 的就地修订）|
| R7-1' | 工具实现模板 + read_file | ✅ 已锁定 | 2026-05-24 | `10-tool-template-and-readfile.md`（含对 05 §5.4 的就地修订）|
| R7-2 | P1/P2 工具 schema | ⏳ 按 Iter-3/4 进度触发 | — | — |
| R8 | 上下文压缩算法 | ⏳ 待开始 | — | — |
| R9 | CLI / REPL | ⏳ 待开始 | — | — |
| R10 | Web UI 后端 API | ⏳ 待开始 | — | — |
| R11 | 前端架构 | ⏳ 待开始 | — | — |
| R12 | 日志与 Trace 格式 | ⏳ 待开始 | — | — |
| R13 | MCP Client | ⏳ 待开始 | — | — |
| R14 | 收敛 + 端到端时序图 | ⏳ 待开始 | — | — |

---

## 三、迭代任务清单（详细）

### Iter-0 · 工程骨架

| 任务 | 状态 | 负责 | 产出 | 验收 | 备注 |
|---|---|---|---|---|---|
| T0.1 初始化 Go 模块与目录骨架 | ✅ | AI | `cmd/mini-agent/main.go` + 38 个 `internal/*/doc.go` + `web/` 骨架 + `scripts/` + 完善的 `.gitignore` + 根 `README.md` | `go build ./...` ✅ / `go vet ./...` ✅ / `go run ./cmd/mini-agent version` ✅ | 删除空占位 `engine/` `studio/`；module=`github.com/HBulgat/mini-agent`，Go 1.23 |
| T0.2 引入主依赖 | ✅ | AI | 13 个直接依赖下载到 module cache（openai-go / anthropic-sdk-go / pkoukk/tiktoken-go / invopop/jsonschema / cobra / viper / gin / lumberjack / doublestar / mvdan/sh / modernc/sqlite / golang-migrate / google/uuid / testify）；go.mod `go 1.25.0`（jsonschema v0.14 触发 Go ≥ 1.24 升级）；GOPROXY 临时切换到 `goproxy.cn,direct`（开发完恢复 woa 内网） | `go build ./...` ✅ / `go vet ./...` ✅ | 选 (a) 方案：T0.2 不锁，go.mod 维持最小态；后续任务 import 时 `go mod tidy` 会自动锁版本——所有依赖已在 module cache，无需再联网 |
| T0.3 Makefile | ✅ | AI | `Makefile`（22 个任务：help/build/run/serve/install/test/test-race/cover/vet/staticcheck/fmt/tidy/lint/check/migrate/sqlc/update-tool-goldens/web-dev/web-build/web-install/clean）；ldflags 注入 version/commit/buildTime；sqlc 与 staticcheck 通过 `go run @version` 调用免本地安装；补 `.gitignore` 加入 `/bin/` | `make help` ✅ / `make build` 产出 `bin/mini-agent` 2.4 MB ✅ / `make vet` ✅ / `make test` ✅ / `./bin/mini-agent version` 输出 `mini-agent 0.0.0-dev` ✅ | sqlc/staticcheck 版本：`v1.27.0` / `2025.1`；Web UI 命令在 T0.7 之后才能跑 |
| T0.4 配置加载 | ✅ | AI | `internal/config/{config,load,apply_flags,mask}.go` 4 文件 + 15 单元测试 + cmd 层 4 集成测试；完整类型树覆盖 11 个顶层 key（LLM/Agent/Context/Permission/Storage/Log/Web/AgentsMD/Skills/UI/MCP）；`Load(path)` 三层优先级（默认值 viper.SetDefault → ~/.mini-agent/config.yaml or --config → CLI flag）+ 路径 `~` 展开；`ResolveMode` 处理 `--yes`/`--auto-edit`/`--plan` 三互斥；`ParseModelRef` 解析 `provider:model` / 裸 model；`ApplyFlags` 叠加 + `validate()` 校验（mode/thinking_effort/max_steps/ratios/web.port）；`(*Config).String()` 脱敏 yaml（OpenAICompat[]/Anthropic/Gemini api_key 全部 mask 为 `********`，深拷贝避免污染源）；`PersistentPreRunE` 接入 cobra 自动加载，`version` 子命令通过 `skipConfigAnnotation` 豁免；错误打到 stderr 带 `mini-agent:` 前缀；锁定 `viper v1.21.0` + `yaml.v3 v3.0.1` | `go test ./internal/config/...` 15/15 ✅；`go test ./internal/cli/cmd/...` 9/9 ✅（含 PersistentPreRunE 4 个集成测试）；`make vet` ✅；`./bin/mini-agent --yes --plan migrate` exit=1 输出冲突错误 ✅；`./bin/mini-agent --config /bad migrate` exit=1 输出文件错误 ✅；`./bin/mini-agent --config /bad version` exit=0（豁免）✅ | D25 不读环境变量；敏感字段 mask 校验通过；`pricing_overrides` 与 `mcp.servers` 已建好 schema 等 R13 / 实化期填充 |
| T0.5 日志封装 | ⏳ | — | — | — | 依赖 R12 |
| T0.6 cobra root + version | ✅ | AI | `internal/cli/cmd/{root,version,migrate,serve}.go` 完整 cobra 命令树 + 5 个单元测试（`cmd_test.go`）；`cmd/mini-agent/main.go` 瘦身为 ldflags 转发入口（调 `clicmd.SetBuildInfo` + `clicmd.Execute`）；遵循 D8（root/serve/migrate/version 四子命令）+ D9（root 上 `-p`/`--print` 一次性）+ D15（migrate 子命令骨架，自动 Up 留 T1.5）；6 个根持久 flag：`--config` / `--model` / `--cwd` / `--yes` / `--auto-edit` / `--plan`；`serve` 子命令独占 `--host` / `--port`；占位子命令以非零退出码（exit=1）兜底防止误用；锁定 `github.com/spf13/cobra v1.10.2` 进 go.mod | `go build ./...` ✅ / `go vet ./...` ✅ / `go test ./internal/cli/cmd/...` 5/5 ✅ / `./bin/mini-agent version` 输出 4 行（version/commit/build/go runtime）✅ / `./bin/mini-agent --help` 列出 4 个子命令 ✅ / `./bin/mini-agent migrate` exit=1 ✅ / `./bin/mini-agent serve --help` 显示 `--host` / `--port` ✅ | REPL 内核（T1.7）/ migrate 实化（T1.5）/ serve 实化（T5.4）替换 placeholder 时需同步更新 `TestPlaceholderSubcommandsExitNonZero` |
| T0.7 前端工程骨架 | ✅ | AI | `web/` 完整 Vite 5 + React 18 + TS 5 + AntD v5 + `@ant-design/x` 1.x 工程：`package.json`（5 dep + 10 devDep）+ `vite.config.ts`（开发代理 /api → 127.0.0.1:7777，含 vitest 段）+ `tsconfig.json`/`tsconfig.app.json`/`tsconfig.node.json` 三件套 + `index.html` + `.npmrc`（项目级镜像 npmmirror）+ `.gitignore`（dist/node_modules/.vite 等）+ `README.md` + `src/main.tsx`（StrictMode + ConfigProvider + AntdApp）+ `src/App.tsx`（Layout/Header/Card/Bubble 占位）+ `src/styles/global.css` + `src/test/setup.ts`（jest-dom 匹配器）+ `src/__tests__/App.test.tsx`（vi.mock 解决 antd ESM 目录导入难题，2 个测试）+ `src/vite-env.d.ts`（CSS/图片资源类型声明）；锁定 `pnpm-lock.yaml`（246 包）；Makefile 新增 `web-test` / `web-typecheck` 两个目标 | `pnpm lint:types` ✅；`pnpm build` ✅（623 kB / 200 kB gzip，2.18s）；`pnpm test` 2/2 ✅（0 warning / 0 error）；`make web-typecheck` / `make web-test` / `make web-build` 全过；`git status` 未把 `node_modules` 纳入跟踪；Go 工程 `make vet` / `make test` 不受影响 | 选型按 R1 §1.2 + meta-decisions 锁定（zustand/react-query/react-router/shiki 留待 Iter-5/6 真正用到时再装）；antd v5.29 ESM 目录导入与 Vitest 2.1 兼容性问题用 `vi.mock` 兜底（实化 UI 测试在 T6.7 用 Storybook） |

### Iter-1 · LLM + 基础工具 + 会话持久化

| 任务 | 状态 | 备注 |
|---|---|---|
| T1.1 trace 事件类型 | 🔄 部分（接口骨架）| AI | `internal/trace/trace.go` 完整类型层（16 个 Type 常量 + 4 级 Level + Event 结构 + Recorder 接口 + WithTrace/FromContext ctx 帮助 + NopRecorder）；伴随 T1.2 落地 | 5 单元测试覆盖 round-trip / 空 ctx / Nop / 字段完整性 / 级别顺序 | 仍待 R12 完成 Fields schema + 内置 slog handler 落盘到 `internal/logs/` |
| T1.2 uio.Sink + uio.Prompter 接口 | ✅ | AI | `internal/uio/{uio,nop,multi,doc}.go` 完整 R2 §5.2 锁定签名（Sink 8 方法 + Prompter 3 方法 + Role/RiskLevel/ApprovalDecision 三枚举 + ToolCallStartEvent/ToolCallEndEvent/ApprovalRequest/QuestionRequest/ChoiceRequest 五数据类型）；`NopSink`（默默丢弃）+ `NopPrompter`（默认拒绝 + 暴露 `ErrNoPrompter` 哨兵 + 优先返回 ctx.Err 区分中断与无 prompter）；`MultiSink` 多路分发（nil 过滤 / nil 接收者安全 / panic 不吞）；同时落地 **T1.1 trace 早期切片**（`internal/trace/{trace,doc}.go` 完整 16 个 Type 常量 + Level 4 级 + Event 结构 + Recorder 接口 + WithTrace/FromContext ctx 帮助 + NopRecorder）—— uio.Sink.EmitTrace 强依赖 trace.Event，必须同步落地 | `go test ./internal/trace/... ./internal/uio/...` 14/14 ✅（trace 5 + uio 9：包含 round-trip / nil 接收者 / ctx 中断优先 / 多路分发顺序 / nil 过滤 / panic 传播 / 枚举顺序 / NopPrompter 默认拒绝）；`make vet` ✅；`go build ./...` ✅；go.mod 0 新增依赖（uio/trace 都是 stdlib + 内部包）| trace 包标记 R12 仍待定 Fields schema 与 JSON 落盘格式，但接口本身已锁；T1.1 在 R12 锁定后只需再补 `internal/logs` 包的 slog handler |
| T1.3 llm.Provider 接口 | ✅ | AI | T1.5 时已落 `internal/llm/llm.go` 接口骨架；本轮补全 `internal/llm/registry.go`（Registry 接口 + memRegistry 实现 + SplitModelRef + 按 prefix 与 Capabilities.Model 双路 lookup + 重复名拒绝 + 模糊引用 `ErrAmbiguousModelRef`）+ `internal/llm/network/{errors,retry,timeout,doc}.go`（HTTPError + 泛型 `WithRetry[T]` 指数退避+jitter+Retry-After 优先 + 双层超时 + shouldRetry 5xx/429/408/net.Error）+ `internal/llm/tokenest/{tokenest,charratio,doc}.go`（Estimator 接口 + CharRatio 中英文加权近似，redacted_thinking 64 token 固定开销）；27 测试覆盖 | `go test ./internal/llm` 13/13 ✅；`go test ./internal/llm/network` 14/14 ✅；`go test ./internal/llm/tokenest` 7/7 ✅ | 不引 tiktoken-go（为避免 OpenAI BPE 词表大文件，CharRatio 已能满足"压缩触发"决策；T6.x P2 阶段可补） |
| T1.4 OpenAI 兼容 provider | ✅ | AI | `internal/llm/openai/{config,pricing,thinking,codec,stream,provider,doc}.go` 7 文件完整实化：Config（多实例 BaseURL+APIKey+DefaultModel+ForceThinking+网络层）+ Validate + withDefaults；modelTable 6 模型（deepseek-chat/reasoner、gpt-4o/4.1、o3/o3-mini）含 Capabilities + 单价（USD per MTok）+ ComputeCost §8.8.2 公式 + LookupModel/defaultCapabilities；mapThinkingEffort 完成 §8.4.1 4 档映射；canonicalToolChoiceToParam 4 模式映射；canonicalMessagesToParams 处理 system/user/assistant/tool 四种 role + 嵌入式 tool_use/tool_result 拆解 + thinking 块在出站时 drop（D36）；buildAPIRequest 含 ReasoningEffort + StreamOptions.IncludeUsage + Tools/ToolChoice；streamConsumer：reasoning_content 用 raw JSON 旁路提取（SDK v1.12 不暴露 DeepSeek 扩展字段）+ tool_calls 按 index 累加 partialCall + transitionTo 合成 BlockBoundary + extractReasoningCached 兼容 OpenAI/DeepSeek 双 usage 格式 + mapStopReason 5 档；Provider New/Name/Capabilities/Stream/EstimateTokens/SetModel + Stream 用 network.WithRetry + WithTimeout 包装；锁 `openai-go v1.12.0` | `go test ./internal/llm/openai` 22/22 ✅（含 mapThinkingEffort 6 子用例 / pricing & cost 3 / codec 6 / stream helper 5 / provider lifecycle 6）；`make vet` ✅；`make test` 全过 | 网络层已就位但运行期未连真实 API（需要 API key 才能验证流式），T1.9 端到端冒烟阶段才走真实 API |
| T1.5 SQLite 仓储 | ✅ | AI | `internal/llm/llm.go`（T1.3 早期切片：ContentBlock/Message/Role/Usage/StopReason/ToolSpec/ToolChoice/Request/StreamEvent/Capabilities/Provider 接口骨架）+ `internal/session/session.go`（域模型 + Visibility/UserVisibility 双轴 + Repository 接口 + ToLLM/FromLLM/WithUserVisibility/WithSourceProvider）+ `internal/session/migrations/{0001_init,0002_add_usage_cache_columns}.{up,down}.sql` 4 文件 + `migrations/migrations.go`（go:embed 不能跨目录，把 embed.FS 拉到同包暴露）+ `sqlc.yaml` + `internal/session/store/queries/{sessions,messages,todos,usage}.sql`（每条 SQL 末尾分号独立成行，规避 sqlc v1.31 wasm 后端的 SQL 末尾截断 bug）+ `internal/session/store/gen/` 6 个 sqlc 自动生成文件 + `internal/session/store/{db,store,codec,fs,doc}.go` 完整 Repository 实装（Open + PRAGMA[WAL/foreign_keys=ON/busy_timeout=5000] + 嵌入式 migrate.Up + UUIDv7 自动填充 + JSON 编解码 ContentBlock + interface{} → int64/float64 类型断言 + ApplyCompaction/ReplaceTodos 事务 + 高频路径无事务）+ `internal/cli/cmd/migrate.go` 实化（接管 placeholder，调 `store.OpenAndMigrate(cfg.Storage.DatabasePath)`）；锁定 6 个直接依赖：`cobra v1.10.2 / viper v1.21.0 / yaml.v3 v3.0.1 / migrate v4.19.1 / uuid v1.6.0 / modernc/sqlite v1.50.1` | `go test ./internal/session/...` 11/11 ✅（迁移幂等 / cache 三列 / Create+Get / NotFound / Append+三种 List 过滤 / 自动 SeqNo / ApplyCompaction 原子+不物理删除 / ReplaceTodos 全量覆盖 / Usage 聚合 / thinking signature 完整往返 / ToLLM+FromLLM / Context 取消）；`go test ./internal/cli/cmd/...` 全过（含 `TestMigrateCommand_RealRun` 真实迁移端到端）；`make vet` ✅；`./bin/mini-agent migrate` 在隔离 HOME 下输出 `schema version was 0 (now current)` exit=0 | sqlc 因 cgo+pg_query 在 macOS 编译失败，临时方案：`CGO_ENABLED=0 go install ...@latest` 走 wasm 后端（已写入 Makefile 的 `SQLC` 变量）；`MarkMessagesArchived` 因 sqlite 不支持 `sqlc.slice('ids')` 改成 `MarkMessageArchived` 单条更新（compaction 批量在 store 层循环调用） |
| T1.6 read_file（按 R7-1' 模板）| ✅ | AI | `internal/tool/{tool,errors,registry,doc}.go` 包基础（Tool 接口 5 方法 + Result 4 字段 + Error/ErrorCode 9 种 + Category 5 种 + Mode 4 种 + memRegistry 含 D84 启动期校验 + Plan 模式过滤 + ToSpecs）；`internal/tool/testkit/{suite,doc}.go` 共享 testify/suite（4 公共测试：SchemaNonEmpty/InvalidArgsRejected/CtxCancel/SchemaGolden + UPDATE_TOOL_GOLDENS=1 自动写 golden）；`internal/tool/fs/{readfile,doc}.go` 完整实现按 §10.3：ReadFileArgs typed struct + invopop/jsonschema reflect (`ExpandedStruct + DoNotReference + RequiredFromJSONSchemaTags + Anonymous + AllowAdditionalProperties=false`) + decodeArgs (DisallowUnknownFields) + validateArgs + resolvePath（cwd 注入 callback）+ probeBinary（NUL byte / 8KB）+ splitLines（保留 wc -l 语义）+ joinWithLineNumbers（6 位行号 + 字节预算）+ buildContent（`<file>` 标签 + 警告内嵌）+ buildDisplay（`read_file <name> (<size>, <lines> lines) → showing <range> [truncated]?`）+ humanizeBytes；`testdata/read_file.schema.golden.json` golden 文件；Makefile 修复 `update-tool-goldens` 的 `-run /TestSchemaGolden` 让它能匹配 testify suite 子测试；锁 `invopop/jsonschema v0.14.0` + `stretchr/testify v1.11.1` | 25 测试全绿（tool 9 + readfile 16，含 testkit 公共 4 + 11 read_file 专属：小文件 happy path / 行截断 ForcedTruncated / 字节截断 ForcedTruncated / 用户主动 UserLimited / 不存在 ErrNotFound / 二进制 ErrInvalidArgs / 空 path / 负数 offset / 未知字段 / max_bytes 硬上限 1MB 自动 clamp / ctx 中断容忍）；`make vet`/`make test` 全过；`make update-tool-goldens` 验证可写 golden | 关键设计决策：tool.Mode 内嵌（避免与 permission 循环依赖）；ctx 在 read_file 中显式 select 不必要（D63 §10.2 R5 注：短同步操作可视为自然完成快于 ctx 取消，仅在 IO 步骤间检查 ctx.Err() 即可）；schema 中 `$schema` URI 字段是 invopop 默认行为，golden 已包含 |
| T1.7 REPL 主循环 | ⏳ | 依赖 R9 |
| T1.8 bootstrap v1 | ✅ | AI | `internal/bootstrap/{bootstrap,doc}.go` 完整 v1：`App` 结构体（Config/Logger/Trace/Repository/LLM/Tools/Sink/Prompter + LIFO closers 栈）；`BootstrapV1(cfg) (*App, error)` 6 步装配（newDefaultLogger → trace.NopRecorder → openRepository（mkdir + OpenAndMigrate + store.New 包装）→ buildLLMRegistry（遍历 OpenAICompat[] → openai.Config + 网络层投影 + Validate + openai.New + Registry.Register；空数组直接 fail；ActiveModel 解析失败时降级到第一注册）→ buildToolRegistry（注册 read_file 含 cwd callback）→ NopSink/NopPrompter 兜底）；`expandUserPath` 处理 `~/`/`$HOME/`；`Close()` LIFO 反向释放 + nil 安全 + 幂等；失败路径自动 cleanup 已构造的资源。`internal/cli/cmd/root.go` 的 placeholder runREPL 接入 BootstrapV1 输出装配诊断（database/providers/tools/active 四行）后仍按 placeholder 契约非零退出（保持 TestPlaceholderSubcommandsExitNonZero 通过）| `go test ./internal/bootstrap/...` 12/12 ✅（expandUserPath 5 子用例 / Happy path / 无 provider 失败 / 缺 APIKey 失败 / nil cfg 失败 / Close 幂等 / nil App.Close 安全 / 自动创建多级数据库父目录）；`make vet`/`make test` 全过 | T1.8 v1 仅装配现已实化的模块；agent.Loop / permission.Gate / skill.Loader / compaction / agentsmd / cli/repl 等待对应 Iter-2/3/4 任务完成后再追加为 App 字段；`buildToolRegistry` 的 cwdProvider 当前用 `os.Getwd` 兜底，T2.6 需改为读 session.Cwd |
| T1.9 端到端冒烟 | ✅ | AI | `internal/bootstrap/e2e_smoke_test.go`：3 轮 ReAct 端到端冒烟 — `read_file{README.md} → list_dir{.} → "Summary: ..."`。**绕过 cli/repl 直接驱动 `app.Agent.Run`**（cli/repl 卡 R9）。fixture：BootstrapV1 全装配 + 临时工作目录含 README.md/AGENTS.md/go.mod + 真实 SQLite Repository + 真实 9 工具 + 真实 permission.Gate(ModeYes) + 真实 agentsmd.Loader + scriptable mockProvider 注入 LLM Registry 后 SetActive("mock:mock-1") + 重建 agent.Loop 注入 mock。captureSink 捕获 ToolCallStart/End。**8 大端到端断言**：(a) StopReason=end_turn + Steps=3；(b) mockProvider.Stream 触发 3 次；(c) 三轮 system 消息都含 `<project_guidelines>` 与 "Always be concise."（D55 系统消息每轮重建）；(d) Sink.toolStarts=2 + toolEnds=2 + 顺序 read_file→list_dir + Err=nil；(e) 第 2 轮 tool_result 真实包含 "Project Title"（read_file 真读了 README）；(f) Repository 持久化 6 条消息（user + 3 assistant + 2 tool_result）+ 第 1 条是 user + 最后 assistant 含 "Summary:"；(g) Sink.errs 为空；(h) Usage 累加 PromptTokens=150/CompletionTokens=60。**发现并修复 Loop bug**：Loop v1 之前未持久化 `RunInput.UserMessage`（只加进 history 送 LLM），导致 Resume / `/show` / Web 转录视图丢失用户输入；修复为 `prepareInitialHistory` 后调 `SessRepo.AppendMessage` 用 `session.FromLLM` 包装；同步更新 3 个旧 loop 测试期望值（+1 user 消息：1→2 / 3→4 / 4→5）| **8 大断言全过** + 4 个旧 loop 测试期望值更新；agent 包测试 36→36（旧测试期望调整后仍为 36）+ bootstrap 包新增 1 个 e2e（含 3 mock turns + 2 真实 tools + 真实 SQLite + 真实 Gate + 真实 AGENTS.md）；18 个包 `make vet`/`make test` 全过 | **关键意义**：(1) 验证 BootstrapV1 → agent.Loop → tool → permission → SQLite → AGENTS.md 全链路在 zero-network 环境下稳定可跑；(2) 暴露并修复了 Loop v1 的 user message 持久化遗漏；(3) 为 T2.7 cli/repl 和 T2.9 四模式验收准备好了"鉴定基准"——任何后续改动都可对照本测试是否破坏端到端契约。CLI 仅接管输入输出，Agent 行为已验证 |

### Iter-2 · Agent Loop + 完整工具集 + 权限

| 任务 | 状态 | 备注 |
|---|---|---|
| T2.1 tool.Tool / Registry | ✅（T1.6 期已落地）| AI | T1.6 期落地 `internal/tool/{tool,errors,registry,doc}.go`：Tool 接口 5 方法 + Result 4 字段（含 UserLimited/ForcedTruncated）+ Error 9 ErrorCode + Category 5 + Mode 4 + memRegistry 含 D84 启动期校验（name 非空 / Description 非空 / Schema 非空且含 type / 重复名拒绝）+ Plan 模式 ListAvailable 自动过滤 Write/Execute/Network + ToSpecs 转 `[]llm.ToolSpec`；附带 `internal/tool/testkit/suite.go` 共享 testify/suite（4 公共测试 + UPDATE_TOOL_GOLDENS=1 自动写 golden 模式）| `go test ./internal/tool/...` 9 个 + 16 个（read_file 含 testkit 共用）= 25/25 ✅ | tool.Mode 内嵌（避免与 permission 循环依赖）；permission.Mode 通过 `type Mode = tool.Mode` 别名复用 |
| T2.2 permission.Gate + 模式 | ✅ | AI | `internal/permission/{permission,rules,hard_denylist,matcher,matrix,gate,doc}.go` 7 文件完整实化（R5 §5.5 + R4 §7.3 + §7.4）：`permission.Mode = tool.Mode` 别名（消除二义性，依赖方向 permission→tool 单向）+ Operation 5 字段 + Decision 4 态 + Result + RuleType/Granularity + Gate 接口 5 方法；`UserRule` 含自定义 `UnmarshalYAML`（type/granularity/pattern/reason 四字段 + 严格枚举校验）；`HardDenyRule` 包内置 starter set 15 条（rm -rf 6 变体 / 2 fork bomb 拼写 / 3 系统文件 redirect / 3 路径级写）；`Substitutor` 处理 `${cwd}`/`${home}`/`~`（含未配置时保持字面 fail-closed）；`matchUserRule` 三粒度（GranTool 完全相等 / GranCommand bash-only doublestar / GranPath 变量展开后 doublestar）+ ToolName 作用域 scope；`matchHardDeny` 双 pass（patternHasGlobMeta 判定 → 字面相等 OR doublestar Match）+ `normaliseSpaces` 折叠空白以匹配 `:(){...}` fork bomb；`matrixDecision` 实化 §4.3 完整 5 mode × 5 cat 矩阵；`gate` struct 含 sync.RWMutex + Check 5 步流程（硬黑→user deny→user allow→matrix→Prompter）+ CheckRulesOnly 仅前 4 步 + SetMode/GetMode 线程安全 + RememberApproval（approvalKey 优先级 path > command > sha256(args)）+ SetCwd 运行时刷新 substitutor + 非零 ctx 即拒 + nil prompter fail-closed；`LoadRules` 文件不存在 fail-soft（只装 hard 黑名单）；锁 `bmatcuk/doublestar/v4 v4.10.0`；同步增强 bootstrap：App 加 `Permission` 字段 + step 5.5 `buildGate` + `mapPermissionMode` (config.PermissionMode→tool.Mode) + root REPL 诊断输出加 mode 行 | `go test ./internal/permission/...` 49/49 ✅（Substitutor 8 子用例 / matchUserRule 4 / matchHardDeny 4 / matrixDecision 16 / CheckRulesOnly 5 / Check 6 / SetMode 1 / LoadRules 4 / Enum 1 / approvalKey 4 / NewGate guard 1）；bootstrap 测试加到 20（含 5 mode 投影子用例 + missing rules file）；`make vet`/`make test` 全过 | 硬黑名单 R7 完整规则集 / bash 命令 shellwords 解析 / RememberApproval 等价性精细化 留实现期（T2.4 bash 工具）|
| T2.3 write_file / edit_file / delete_file | ✅ | AI | 三个 Write 类工具按 R7-1' §10.7 模板完整实化：**`write_file`**（WriteFileArgs 三字段 path/content/mkdir_parents `*bool` 三态 + 1 MB 硬上限 + 原子写：`os.CreateTemp` 在同目录建临时文件 → 写入 → `tmp.Sync()` fsync → `chmod 0644` → `os.Rename` 同 fs 原子，失败路径 best-effort cleanup `.mini-agent-write-*.tmp`）；**`edit_file`**（EditFileArgs 四字段 path/old_str/new_str/expected_occurrences `*int` + 2 MB 文件硬上限 + `strings.Count` 严格计数 + 实际命中 ≠ expected 返回 `ErrAmbiguous` 含前 3 处 1-based 行号 + 0 处返回 `ErrNotFound` + `findOccurrenceLines` 单遍 `bytes.Index` + `bytes.Count` 计算行号 + 拒绝 directory / old_str==new_str / expected≤0 / 文件不存在）；**`delete_file`**（DeleteFileArgs 单字段 path + 拒绝 directory + 拒绝非 regular 文件 + symlink 仅删 link 不删 target）；三个 golden 文件由 `make update-tool-goldens` 自动生成；read_file 的 `resolvePath` 方法重构为调包级 `resolvePath` 函数让所有 fs 工具复用；bootstrap 升级为按字母序注册 4 个工具（read/write/edit/delete），test 改用严格 roster 断言；新增公共函数 `atomicWrite` / `findOccurrenceLines` / `pluralS` / 包级 `resolvePath` | 53 个新测试（write 14 / edit 22 含 findOccurrenceLines 8 子用例 / delete 9 / bootstrap 改 8）；fs 包累计 69 测试全过；`make vet`/`make test` 全过；`make update-tool-goldens` 三个 golden 自动生成且与现版本一致 | bash 工具的命令字符串解析 + 硬黑名单完整规则集留 T2.4；list_dir / grep / glob 留 T2.5（按相同模板填空，工作量类似）|
| T2.4 bash + 硬黑名单 | ✅ | AI | **bash 工具完整实化**（按 R7-1' §10.7 + §10.9 留白补齐）：`internal/tool/shell/{bash,doc}.go` + `bash_test.go` + `testdata/bash.schema.golden.json`；BashArgs 5 字段（command 必填 / cwd 可选 / stdin 可选 / timeout_sec `*int` 默认 60 上限 300 / env_overrides map）；走 `/bin/bash -c <command>` 让 shell 自己处理 `&&`/`\|\|`/`\;`/`\|`/`$()`/反引号（不自实现 shellwords，工作量 vs 价值不划算）；`Setpgid:true` + `kill(-pgid, SIGKILL)` 进程组级 kill 防止 children 脱离；`cappedBuffer` io.Writer 50KB 上限 + sync.Mutex 并发安全 + 上限后 silent accept 防止 writer loop；ctx 取消 → ErrInterrupted；ctx timeout → ErrTimeout；非零 exit 不报错（Result 含 exit_code 让 LLM 决定）；env_overrides 空值 = 删除变量；cwd 相对路径基于 session.Cwd；clampInt 防 LLM 生成超大 timeout；mergeEnv 实现 base + overrides 合并/覆盖/删除；`<bash exit_code=N><stdout>...</stdout><stderr>...</stderr></bash>` 标签 + truncation warning。**硬黑名单 starter set 15 → 50+ 条**：rm 6 变体（含 `--no-preserve-root` / `-fr` / 含 `${HOME}` 大括号变体）+ 块设备销毁 10 条（dd/mkfs/wipefs/shred 对 sd*/nvme*/hd*/disk*）+ fork bomb 2 拼写 + 系统文件 redirect 14 条（含 tee/`tee -a`/`>>` 变体覆盖 passwd/sudoers/shadow/hosts）+ pipe-to-shell 6 条（curl/wget `\| sh\|bash` + `-o -` 变体）+ chmod/chown 6 条（777/000/-R 在 / 与 /\*）+ git push --force 5 条（main/master 三种语法）+ 路径级 7 条（含 `/etc/hosts` / `/boot/**` / `/root/.ssh/**` 新增）。**关键架构修复**：matchHardDeny 的 bash 命令匹配从 doublestar 改为自定义 `matchBashPattern`（doublestar `*` 不跨 `/`、`**` 仅在 `/path/**/path` 时生效，对命令字符串误判命中——典型反例 `dd if=/dev/zero of=/dev/sda` 与 `curl https://x.com/y \| sh`）；新实现把 `*` / `**` 都当作"任意字符序列"，编译为正则 `.*`，对其它 metachar `.+?(){}\|^$\\` 严格 escape；锁定 `regexp` 进 permission 包；同步增强 bootstrap 注册 8 工具按字母序（bash/delete/edit/glob/grep/list_dir/read/write）+ roster 断言更新 | 30 个 bash 测试（含 testkit 4 + 16 行为：echo/non-zero exit/stderr/stdout cap/stderr cap/timeout/ctx cancel/stdin/cwd/cwd-not-dir/env override/env delete/empty cmd/negative timeout/timeout clamp/unknown field/pipe compound/process group kill + mergeEnv 5 子用例 + clampInt 6 + cappedBuffer 1）；49 个新硬黑名单测试（bash table 31 cases 覆盖所有新规则 + benign cases 7 + path table 11 + size&shape sanity 1）；`make vet`/`make test` 15 个包全过 | 命令字符串硬黑名单匹配从此走 `matchBashPattern`（编译为 anchored 正则）；doublestar 仍用于路径级与 GranPath/GranCommand 用户规则。RememberApproval 等价性精细化、复合命令的 token 级深度审查、AlwaysApprove 持久化等仍留实现期 |
| T2.5 ask_user | ✅ | AI | `internal/tool/ask/{askuser,doc}.go` + `askuser_test.go` + `testdata/ask_user.schema.golden.json`：AskUserArgs（question 必填 / hint 可选）；构造期注入 `uio.Prompter`（接口而非 callback——保持与 R3 uio 设计的对称性，未来能复用 Sink 等其它 uio 能力）；Category=Meta 让 matrixDecision 在所有模式（含 yes/plan）放行（D86）；Invoke 调 `Prompter.AskUser(ctx, QuestionRequest)` 并把答案包成 `<ask_user><question>...</question><answer>...</answer></ask_user>`（含 XML escape `<>&`）；nil prompter fail-closed → ErrInvalidArgs；prompter 错误优先映射 ctx，否则 ErrIO；snippet helper 60 字符 trace truncation；bootstrap 流程重排：把 NopPrompter/NopSink 装配从 step 6 提前到 step 4.5，确保 step 5 的 buildToolRegistry 能拿到非 nil prompter；`buildToolRegistry(cfg, prompter)` 签名扩展；按字母序注册 9 工具（ask_user/bash/delete/edit/glob/grep/list_dir/read/write）；bootstrap_test roster 断言更新 | 17 个 ask_user 测试（testkit 4 + 行为 9：echo answer / pass hint / single invoke / empty question / missing question / unknown field / nil prompter fail-closed / prompter error → ErrIO / Category=Meta / XML escape）+ pure-fn 4（snippet 4 + escapeForTag 6 子用例合并算 1 测试 × 多 case）；ask 包 17/17 ✅；bootstrap 测试加 1（roster 9 工具）；`make vet`/`make test` 16 个包全过 | 至此 P0 工具集 9 个完整就绪（ask_user / bash / delete_file / edit_file / glob / grep / list_dir / read_file / write_file），可进入 T2.6 agent.Loop 编排 |
| T2.3+ 只读工具补全（list_dir / grep / glob，§10.7 P0 表）| ✅ | AI | dev-plan 未单列，本任务作为 T2.3 自然延伸完成 P0 ReadOnly 工具集：**`internal/tool/fs/listdir.go`**（ListDirArgs path/recursive `*bool`/ignore_patterns `*[]string` + 默认 ignore set 12 条 `.git/`/`node_modules/`/`__pycache__/` 等 + 用户传入则完全替换默认 + `[]` 显式禁用过滤；非递归只列 ReadDir / 递归走 `filepath.WalkDir`+SkipDir+SkipAll；总条数硬上限 5000 → ForcedTruncated；按 rel path 字典序排序；`matchIgnore` 双 pass 设计（dirOnly slash + 路径含 `/` 走 doublestar full path / 否则 basename match + 中段 segment 比对让 `node_modules` 命中 `src/node_modules/foo`）；`<dir path entries>` 标签 + 三类型行 `d`/`-`/`l` + size 列）；**`internal/tool/search/grep.go`**（GrepArgs pattern/path/output_mode/multiline `*bool`/context_before/after `*int`；RE2 正则；三 output_mode：content（默认 `<path>:<line>:<text>` matches，`<path>-<line>-<text>` context）/files_with_matches/count；multiline=true 自动前缀 `(?s)`；context window merge 避免重叠 padding；文件级硬上限 10MB（skipped + warning）/总匹配硬上限 1000/总文件硬上限 5000；走与 list_dir 共享的默认 ignore set；显示 ripgrep 风格 `<grep path mode files matches>` 包裹 + 截断/oversize warning）；**`internal/tool/search/glob.go`**（GlobArgs pattern/cwd 可选；doublestar v4 ValidatePattern + GlobWalk + SkipDir 截断；`splitAbs` 处理绝对 pattern → root + relPattern 分离；总数硬上限 5000；按字典序排序；**故意不应用默认 ignore**——glob 的目的就是命中文字面 pattern）；统一为每个工具生成 testdata schema golden + 接入 testkit suite；bootstrap 升级为按字母序注册 7 工具（delete/edit/glob/grep/list_dir/read/write）；新增 search 包（`internal/tool/search/{grep,glob,doc}.go`），含本地副本 `resolvePath` / `schemaToMap` / `matchIgnore` 避免 fs 包反向依赖 | 53 个新测试（list_dir 13 含 testkit 4 + 9 专属 + matchIgnore 10 子用例 + Truncation cap 1；search 包 40：grep 14 含 testkit 4 + content/files/count 三模式 + context lines + invalid regex + ignore set + multiline + oversize；glob 12 + splitAbs 4 子用例 + matchIgnore 3）；fs 包累计 82 测试 / search 包 40 测试；`make vet`/`make test` 14 个包全过；`make update-tool-goldens` 三个新 golden 自动生成 | bootstrap 中 cwdProvider 仍走 `os.Getwd`（T2.6 改 session.Cwd）；search 包内 `matchIgnore` 与 fs 包同算法独立副本（避免 fs↔search 互依赖；权衡：维护成本 vs 包独立性）|
| T2.6 agent.Loop | ✅（v1）| AI | `internal/agent/{agent,signature,failure_counter,prompts,deps,interrupt,exec,loop,doc}.go` + `prompts/system.md`（go:embed）+ test fixtures。**Loop 类型族**：Runner 接口；RunInput 5 字段（SessionID/UserMessage/Sink/Prompter/IsResume）；RunResult 4 字段（StopReason/Steps/Usage/LastError）；StopReason 4 态（end_turn/max_steps/interrupted/error）；Config 4 字段含 CompactionConfig；Deps 8 字段（Provider/Registry/PermGate/SessRepo/Recorder + 三 Nop 占位 Compactor/SkillLoader/AgentsMDLoader）；`New(deps,cfg)` 必填 dep 校验返 MissingDepError + Nop 兜底缺省 dep + withDefaults 填零字段。**signature.go**：D56 `<tool>:<sha256(canonicalJSON(args))[:8]hex>` 16 字符；canonicalJSON 递归排序 map keys；不规范化路径让 `./go.mod` 与 `go.mod` 不同 sig（D56 + C2）。**failure_counter.go**：D57 sync.Map[string]*atomic.Int64 + Increment/Reset/Get；并发安全。**prompts.go + prompts/system.md**：D49–D51 ~110 词内置 system prompt（go:embed）+ text/template 渲染 cwd/OS/RFC3339 time；BuildSkillList D54 第 2 槽 + BuildAgentsMD D52 `<project_guidelines>` 包裹；空输入返空字符串让调用方跳过注入。**deps.go**：占位接口 Compactor/SkillLoader/AgentsMDLoader + 各自 Nop 实现（Iter-3/4 落实化时不改 Loop API）；CompactionResult 描述压缩元数据。**interrupt.go**：synthesizeInterruptedResult D64 `[interrupted before tool was invoked]` + IsError=true 让历史协议合法；toolResultMsg user+ToolResult 块；extractToolUseBlocks 从 assistant 消息按出现顺序抽 tool_use。**exec.go**：D58/D59 双桶并行 — readonly bucket 用 sync.WaitGroup 全部跑完（不取消兄弟），sequential bucket 串行；ctx 在 bucket 切换时检查并对未填位置合成 interrupt 占位；executeToolCall 五步流程（lookup → Gate.Check → Sink.EmitToolCallStart → Invoke → Sink.EmitToolCallEnd）；Gate ctx 错误映射 interrupt 占位；DenyHard/Deny 不计入失败计数器（D66 §5.5.5）；失败 → fc.Increment 且 ≥ToolRetryMax 时附 D76 "please try a different way" 提示；buildOperation 抽 path/command 字段并 filepath.Abs 给 gate。**loop.go**：Run 主循环 — prepareInitialHistory（3 系统消息 D54 + Resume 时 ListLiveMessages + UserMessage）→ for step{ ctx 检查 → MaybeCompact → streamOnce → 持久化 assistant → extract tool_uses → 0 tool_use 即 StopEndTurn → executeToolCalls → 持久化 tool_results → ctx 取消即 StopInterrupted } → StopMaxSteps；nil Sink/Prompter 自动兜底 NopSink/NopPrompter；mergeUsage 累加；getCwdFromEnv 走 os.Getwd（T3 改 session.Cwd）。**bootstrap step 6 装配 agent.Loop**：App.Agent 字段 + agent.New 传 Provider/Registry/PermGate/SessRepo/Recorder + Compactor/SkillLoader/AgentsMDLoader 留 nil 用 Nop 兜底 + cfg.Agent 投影 MaxSteps/ToolRetryMax | 32 个 agent 测试（signature 6 + failureCounter 5 + prompts 6 + interrupt 4 + loop end-to-end 9：EndTurnWithoutTools / OneToolCallThenEndTurn / FailingToolHintAfterRetryMax / MaxStepsCap / CtxCancelMidLoop / ProviderStreamErrorBecomesStopError / ParallelReadOnlyBucket / NilSinkPrompter / NewRejectsMissingDeps × 4 子用例）；用 fakeProvider + memRepo + echoTool/failTool + 真实 permission.NewGate(ModeYes) 端到端验证；bootstrap 测试加 Agent 字段断言；17 个包 `make vet`/`make test` 全过 | T2.6 v1 范围：单 agent ReAct 循环 + 失败计数器 + 双桶并行 + 中断 tool_use↔tool_result 配对 + 三独立 system 消息 + Sink 事件流。**留 Iter-3+**：完整 Compactor 实现（R8/T3.x）/ SkillLoader 实现（R10/T4.x）/ AgentsMDLoader 实现（T2.8）/ Spawner+task 工具（D60/D61/D65 子 agent）/ 跨 provider 切换归档思考块（D62 §8.11.4，等多 provider）。Loop 接口稳定；上述模块落地不会破坏 API |
| T2.7 斜杠命令派发 | ⏳ | 依赖 R9 |
| T2.8 AGENTS.md 加载 | ✅ | AI | `internal/agentsmd/{loader,doc}.go` + `loader_test.go`：**Loader 契约**（R4 §7.2.6）`Load(ctx, cwd) (string, error)`；Config 三字段（GlobalPath / ProjectLookup / MaxBytes，零值=DefaultMaxBytes 1 MiB）；New 接受 `*Config` 走 D31 指针约定。**实化 D27 查找**：全局 `cfg.GlobalPath`（默认 `~/.mini-agent/AGENTS.md`，运行期 `expandHome` 处理 `~`/`~/`/`~/foo`）+ 项目级 `<cwd>/AGENTS.md`（仅当 ProjectLookup=true）；**不向上递归**（monorepo 安全）；每次 Load 重读不缓存（让 `/cd` 重读由调用方控制）。**实化 §7.2.2 合并**：`mergeSections` 全局在前 + `\\n\\n---\\n\\n` 分隔 + 项目级在后；TrimSpace 后空字符串视为缺失（避免空文件产生孤立 `---`）。**实化 D53 截断**：`readAtMost` 读 `maxBytes+1` 一次性区分"恰好/超出"，超出截到 maxBytes + 追加 `\\n[...truncated, N bytes total]` 用 `info.Size()` 提示真实尺寸。**实化 §7.2.5 fail-soft**：文件不存在 / 空 / 读失败 / 是目录 / 权限拒绝 → 全部静默返空，不报错（agent.Loop 中 prepareInitialHistory 也对 loader 错误做 fail-soft 包装，加固一致性）。bootstrap step 5.6 装配真实 `agentsmd.New(&agentsmd.Config{...})` 替换 NopAgentsMDLoader 注入 agent.Deps；signature 与 agent.AgentsMDLoader 接口完全匹配，无需适配器 | 32 个 agentsmd 测试（discovery 6：neither / global only / project only / both merged / no upward recursion / project lookup disabled；emptiness 4：empty file / whitespace only / unreadable file（root skip）/ AGENTS.md 是目录；truncation 2：oversize 截断 + 标记 / 恰好等于 cap 不截断；construction 3：nil cfg / default maxBytes / negative maxBytes 走默认；ctx 6：cancelled context；tilde expansion 7 子用例（`""`/`~`/`~/`/`~/foo`/`/abs`/`relative`/`~tilde-prefix-only`）；mergeSections 6 子用例；cwd reload 1）+ 4 个 agent 集成测试（GlobalAndProject 注入 → `<project_guidelines>` 包裹 + 全局先项目级后 `---` 分隔 / Absent 时不注入空 wrapper / Loader error fail-soft 不破坏 Run / 错误时也不漏出 wrapper）；agent 包总测试数 32 → 36；`make vet`/`make test` 18 个包全过 | 缓存策略：loader 自己不缓存——T2.7 `/cd` 命令实现时只需重调 `Load(ctx, newCwd)`；T4.x（webapi）多 session 时也直接 per-call 调用即可，不需要额外失效逻辑 |
| T2.9 端到端：四模式跑标准任务 | ✅ | AI | `internal/bootstrap/four_modes_e2e_test.go`：9 个端到端用例覆盖 §4.3 工具×权限矩阵的代表性单元格。**fixture 模式**：`newFourModeFixture(t, mode, turns, prompter)` 复用 e2e_smoke_test.go 的 mockProvider/captureSink 抽象 + BootstrapV1 → Registry.Register + SetActive + 重建 Loop 注入 mock；approvePrompter / denyPrompter 双 fake 让用例显式声明"如果有审批我同意/拒绝"。**8 个矩阵用例 + 1 个 harness sanity**：(C1) Default+read_file → ReadOnly 免审批（denyPrompter.calls=0 / 工具成功）；(C2) Default+write_file → 用户拒绝 → 文件不写 + Sink 收到 ToolCallEnd 含 Err；(C3) AutoEdit+write_file → 直接放行 + 文件真写入；(C4) Yes+bash 安全命令 → 直接放行不询问；(C5) Yes+bash `rm -rf /` → DenyHard 拒绝 + approvePrompter.calls=0（硬黑名单短路于审批之前）+ 错误消息含 denylist；(C6) Plan+read_file → 放行；(C7) Plan+write_file → DenyMode 拒绝（不询问 prompter）+ 文件不写；(C8) Default+ask_user → Meta 类放行 + AskApproval 不调用（仅 AskUser 被调）；(Sanity) MockProvider+textOnly turn 验证 fixture 自身正确装配。**发现并修复两个 Loop bug**：(Bug-1) Sink 事件契约不一致 — Gate 拒绝（Deny/DenyHard）时 Loop 直接 return tool_result 而**未发 ToolCallStart/End**，UI 侧无法可视化"被拒尝试"；修复：新增 exec.go `emitDeniedToolEvents` helper 在拒绝分支也发 Start+End 对，Err 携带拒绝原因。(Bug-2) ask_user 装配期固化 NopPrompter — bootstrap step 5 把 NopPrompter 锁进 ask_user 构造期，运行期 RunInput.Prompter 用不上；修复：新增 `internal/uio/context.go`（WithPrompter/PrompterFromContext + WithSink/SinkFromContext）让 Loop 在 `t.Invoke(ctx, ...)` 前注入 ctx，ask_user.Invoke 优先取 ctx 里的 Prompter 回退构造期 prompter。这套 ctx 桥接同样支持未来多个并发 Run（CLI + Web）共享同一 tool registry 但用不同 Prompter | 9 个 four_modes 测试全过 + 修复后的 e2e_smoke 仍过 + 全 18 包 `make vet`/`make test` 全过 | **关键意义**：(1) §4.3 矩阵的 4 个权限模式 × 4 个工具类别全部端到端验证，不只是 Gate 单测；(2) 暴露并修复了 Sink 事件契约空洞 + ask_user 多请求并发隐患，两者都是 Iter-3+（webapi）会触发的真实 bug；(3) 至此 Iter-2 仅剩 T2.7（REPL 斜杠命令派发，卡 R9）。**P0 工具集 + agent.Loop v1 + 权限矩阵** 全部端到端验证完毕 |

### Iter-3 · 压缩 + write_plan + task

| 任务 | 状态 | 备注 |
|---|---|---|
| T3.1 Compactor + summarize | ⏳ | 依赖 R8 |
| T3.2 自动压缩触发 + /compact | ⏳ | 依赖 R8 |
| T3.3 write_plan + Todo 仓储 | ⏳ | 依赖 R7-2 |
| T3.4 task 子 agent | ⏳ | R6 ✅ + 待 R7-2 task input schema |
| T3.5 端到端：规划→子agent→汇总 | ⏳ | — |

### Iter-4 · Skill + Trace + 多 Provider + 白黑名单

| 任务 | 状态 | 备注 |
|---|---|---|
| T4.1 Skill 加载器 | ⏳ | R2 ✅ 可开始 |
| T4.2 启动注入 skill 列表 | ⏳ | R1 ✅ |
| T4.3 skill_tool | ⏳ | 依赖 R7-2 |
| T4.4 /skill 命令 | ⏳ | 依赖 R9 |
| T4.5 子 agent 继承 skill | ⏳ | R6 ✅ 可开始 |
| T4.6 压缩后重新注入 skill 列表 | ⏳ | 依赖 R8 |
| T4.7 详细 Trace 落地 | ⏳ | 依赖 R12 |
| T4.8 /trace on/off | ⏳ | 依赖 R9 |
| T4.9 Anthropic / Gemini 适配 | ⏳ | R5 ✅ 可开始（Anthropic P0 / Gemini P1）|
| T4.10 白黑名单 | ⏳ | R4 ✅ 可开始 |

### Iter-5 · Web UI 后端 + 前端骨架

| 任务 | 状态 | 备注 |
|---|---|---|
| T5.1 gin REST API | ⏳ | 依赖 R10 |
| T5.2 SSE 流式接口 | ⏳ | 依赖 R10 |
| T5.3 webapi 端 uio 实现 | ⏳ | 依赖 R10 |
| T5.4 serve 子命令 | ⏳ | R1 ✅ |
| T5.5 前端骨架 | ⏳ | 依赖 R11 |
| T5.6 Markdown / 高亮 / diff | ⏳ | 依赖 R11 |

### Iter-6 · Web UI 完善 + 验收

| 任务 | 状态 | 备注 |
|---|---|---|
| T6.1 工具调用可视化 | ⏳ | 依赖 R11 |
| T6.2 文件树 + 预览 / diff | ⏳ | 依赖 R10/R11 |
| T6.3 Todo / Trace / 配置编辑 | ⏳ | 依赖 R11 |
| T6.4 权限审批卡片 | ⏳ | 依赖 R10/R11 |
| T6.5 Skill 详情面板 | ⏳ | 依赖 R11 |
| T6.6 多 cwd 切换 | ⏳ | 依赖 R10/R11 |
| T6.7 端到端验收 | ⏳ | — |

### Iter-7 · P3（一周窗口外）

| 任务 | 状态 | 备注 |
|---|---|---|
| T7.1 MCP Client | ⏳ | 依赖 R13 |
| T7.2 滑动窗口压缩 | ⏳ | 依赖 R8 |
| T7.3 分层摘要压缩 | ⏳ | 依赖 R8 |
| T7.4 异步子 agent | ⏳ | 需要重新走需求 |

---

## 四、变更日志（Changelog）

| 时间 | 类型 | 内容 | 影响 |
|---|---|---|---|
| 2026-05-23 | 需求 | 新增 Skill 模块补丁，更新 9 处需求文档 | 开发计划 Iter-4 增加 T4.1–T4.6 共 6 项 skill 任务 |
| 2026-05-23 | 设计 | R1 锁定（总体架构 / 关键决定 / UIO / 工具集 / 权限矩阵） | 开发计划基础版可制定 |
| 2026-05-23 | 文档 | 创建 `docs/dev-process/` 目录与初始 plan/progress | — |
| 2026-05-24 | 设计 | R2 锁定（核心抽象：trace/uio/llm/tool/permission/skill/compaction/session/agent 共 9 模块的接口签名） | 解锁 T1.2/T1.3/T2.1/T2.2/T4.1 等 5 项任务的"等待 R2"状态 |
| 2026-05-24 | 设计 | R3 锁定（SQLite schema + Codec + 三层数据结构 + 思考模式 + 可见性）；新增 D11–D24 关键决定；就地修订 `05-core-abstractions.md` 中 llm/uio/trace/compaction/session/agent 多个章节 | 解锁 T1.5（SQLite 仓储）；T1.4 含思考模式适配；T2.6 ReAct loop 含思考 token 处理 |
| 2026-05-24 | 设计 | R4 锁定（配置 + AGENTS.md + 白黑名单 + 指针约定 D31）；新增 D25–D31 关键决定 | 解锁 T0.4（配置加载）/ T2.8（AGENTS.md）/ T4.10（白黑名单）；为后续所有任务确立 Go 代码风格 |
| 2026-05-24 | 设计 | R5 锁定（LLM Provider 适配：OpenAI 兼容/Anthropic 双 P0、Gemini P1；流式契约 + thinking 协议 + cost + 网络层）；新增 D32–D48 关键决定；新建 `08-llm-providers.md`；就地修订 05（Request/Usage/StreamEvent 字段扩充）/ 06（usage_log 加 cache 三列、Codec 接口实化、Anthropic Codec 表更新）/ 07（llm 配置段重构）| 解锁 T1.3（接口实化）/ T1.4（OpenAI 兼容 provider）/ T1.5（SQLite 仓储 cache 列）/ T4.9（Anthropic / Gemini 适配）|
| 2026-05-24 | 设计 | R6 锁定（Agent 执行引擎细化：系统 prompt 模板 + AGENTS.md 包裹 + 失败计数器 sync.Map+sha256 signature + 多 tool_calls 按 Category 分桶并行 + 中断 tool_use↔tool_result 配对 + 子 agent 失败结构化 + 跨 provider 切换 summary + ctx 取消颗粒度）；新增 D49–D67 关键决定；新建 `09-agent-engine.md` + 内置系统 prompt 模板；就地修订 05 §5.9 全部留白 | 解锁 T2.6（agent.Loop）/ T3.4（task 子 agent）/ T4.5（子 agent 继承 skill）|
| 2026-05-24 | 设计 | R7-1' 锁定（工具实现模板 + read_file 完整设计：6 条统一规范 + invopop/jsonschema 反射 + Result 字段调整 + 错误码扩充 + ctx 错误强约定 + testkit 共享套件 + Schema golden file）；新增 D68–D86 关键决定；新建 `10-tool-template-and-readfile.md`；就地修订 05 §5.4（Result 删 Truncated 加 UserLimited+ForcedTruncated；ErrorCode 新增 ErrTooLarge+ErrAmbiguous）；策略上不为每个工具单独走集中设计轮次 | 解锁 T1.6 / T2.3 / T2.4（按模板填空）|
| 2026-05-24 | 开发 | **T0.1 完成**：删除空占位 `engine/` `studio/`；按 R1 §1.2 创建完整目录树（`cmd/mini-agent` + `internal/` 17 个一级模块 + 5 个 `tool/` 子包 + 3 个 `compaction/` 策略子包 + 4 个 `llm/` provider 子包 + `web/` 前端骨架 + `scripts/`）；38 个 `doc.go` 含模块职责与设计文档锚点；`cmd/mini-agent/main.go` 占位（`version` 子命令可用）；完善 `.gitignore`（IDE/Go 产物/data.db/前端 node_modules/.env 等）；根 `README.md` 指向 `docs/` 三套文档 | `go build ./...` 通过；`go vet ./...` 通过；`go run ./cmd/mini-agent version` 输出 `mini-agent 0.0.0-dev` |
| 2026-05-24 | 开发 | **T0.2 完成**：13 个直接依赖下载到 module cache（含 R5/R7-1' 选定的 openai-go/anthropic-sdk-go/tiktoken-go/jsonschema/modernc-sqlite/cobra/viper/gin/lumberjack/doublestar/mvdan-sh/migrate/uuid/testify）；选 (a) 方案不在 T0.2 锁 go.mod —— 后续任务 import 时 tidy 自动补齐；GOPROXY 临时切换到 `goproxy.cn,direct`（开发完恢复内网 woa）；invopop/jsonschema v0.14 要求 Go ≥ 1.24，go.mod 自动升级到 `go 1.25.0`（本机工具链） | 全部依赖已就绪；后续任务无需再联网拉新依赖 |
| 2026-05-24 | 开发 | **T0.3 完成**：`Makefile` 提供 22 个任务覆盖 build/test/vet/staticcheck/fmt/tidy/migrate/sqlc/update-tool-goldens/web-dev 等开发循环；ldflags 注入 version/commit/buildTime；sqlc(v1.27.0) 与 staticcheck(2025.1) 通过 `go run @version` 调用免本地安装；`.gitignore` 补 `/bin/` | `make build` → `bin/mini-agent` 2.4 MB；`make vet` / `make test` 通过 |
| 2026-05-25 | 开发 | **T0.6 完成**：cobra 命令树落到 `internal/cli/cmd/`（root + version + migrate + serve 共 4 个 .go 文件）；`cmd/mini-agent/main.go` 瘦身为 ldflags 转发入口；6 个根持久 flag（`--config` / `--model` / `--cwd` / `--yes` / `--auto-edit` / `--plan`）+ root 局部 `-p`/`--print`（D9）+ serve 独占 `--host` / `--port`；占位子命令以 exit=1 兜底；新增 5 个单元测试覆盖 version 输出 / 根 help / 占位非零退出 / 持久 flag 解析 / serve 帮助；锁定 `cobra v1.10.2` | `go test ./internal/cli/cmd/...` 5/5 ✅；`./bin/mini-agent version` / `--help` / `migrate` / `serve --help` 全部行为符合 D8+D9+D15 |
| 2026-05-25 | 开发 | **T0.4 完成**：`internal/config/{config,load,apply_flags,mask}.go` + 15 个 config 单元测试 + 4 个 cmd 集成测试（PersistentPreRunE 三互斥 / 不存在 --config / version 豁免 / 持久 flag）；11 顶层 key 完整类型树；三层优先级加载（defaults < file < flags）；`~` 路径展开；`ResolveMode` 三互斥校验；`ParseModelRef` 解析 `provider:model` 与裸 model；`(*Config).String()` 通过 deep clone + yaml.Marshal 脱敏所有 api_key（不污染源）；cobra `PersistentPreRunE` 自动加载配置并存入 `flags.cfg`，`version` 子命令以 `skipConfigAnnotation` 豁免；错误统一带 `mini-agent:` 前缀打到 stderr；锁定 `viper v1.21.0` + `yaml.v3 v3.0.1` | 测试 24/24 全绿；`./bin/mini-agent --yes --plan migrate` 输出"--yes / --auto-edit / --plan are mutually exclusive"并 exit=1；`./bin/mini-agent --config /bad migrate` 输出文件错误并 exit=1；`./bin/mini-agent --config /bad version` 仍正常输出版本（豁免）|
| 2026-05-25 | 开发 | **T0.7 完成**：`web/` 完整 Vite 5 + React 18 + TS 5 + AntD v5 + @ant-design/x 1.x 工程；`pnpm-lock.yaml` 锁 246 包（npmmirror 国内源 15s 拉完）；`tsconfig` 三件套（root references → app/node）+ `vite.config.ts`（dev /api 代理 + vitest jsdom）+ `index.html` + `.npmrc` + `.gitignore` + `README.md`；`src/main.tsx`（StrictMode + ConfigProvider + AntdApp）+ `src/App.tsx`（AntD Layout/Card + @ant-design/x Bubble 占位）+ 全局样式 + vite-env 资源类型声明；`vi.mock(antd, @ant-design/x)` 兜底解决 antd v5.29 ESM 目录导入与 Vitest 2.1 兼容性问题（实测 inline/transformMode 等都无效），2 个 React Testing Library 用例验证 React 树健康；Makefile 加 `web-test` / `web-typecheck` 目标；corepack 激活 pnpm@9.15.0（Node 20 自带） | `pnpm lint:types` ✅；`pnpm build` 2.18s 输出 dist/index.html + 623 KB JS（200 KB gzip）；`pnpm test` 2/2 ✅；`make web-*` 全过；Go 工程 `make vet`/`make test` 不受影响 |
| 2026-05-25 | 开发 | **T1.5 完成**：`internal/llm/llm.go`（T1.3 早期切片，11 个 canonical 类型 + Provider 接口骨架）；`internal/session/session.go` 完整域模型（Session/Message/Todo/Usage + Role/Visibility/UserVisibility/SessionStatus/TodoStatus/TodoOwner 6 个枚举 + Repository 接口 13 方法 + ToLLM/FromLLM/WithUserVisibility/WithSourceProvider）；`internal/session/migrations/` 4 个 SQL 文件 + `migrations.go`（go:embed 跨目录绕道方案）；`sqlc.yaml` + 4 个 queries SQL（末尾分号独立成行规避 v1.31 wasm 后端截断 bug，`MarkMessagesArchived` 改单条更新规避 sqlc.slice 在 sqlite 下不支持）；sqlc 自动生成 `internal/session/store/gen/` 6 个文件；`internal/session/store/{db,store,codec,fs,doc}.go` 实装 Repository 接口（Open + 三 PRAGMA + 嵌入 migrate.Up + UUIDv7 自动填充 + JSON 编解码 ContentBlock + ApplyCompaction/ReplaceTodos 事务 + interface{} 聚合列类型断言）；`internal/cli/cmd/migrate.go` 实化为真实迁移命令；锁定 `golang-migrate v4.19.1` + `google/uuid v1.6.0` + `modernc/sqlite v1.50.1` | 14 个测试全绿（11 仓储 + 3 cmd）；migrate 端到端跑通 fresh database；macOS sqlc cgo 问题改用 `CGO_ENABLED=0` wasm 后端解决 |
| 2026-05-25 | 开发 | **T1.2 完成 + T1.1 部分落地**：`internal/uio/{uio,nop,multi,doc}.go` 完整 Sink+Prompter 接口（R2 §5.2 锁定签名）+ NopSink/NopPrompter（默认拒绝+ErrNoPrompter 哨兵）+ MultiSink 多路分发（nil 过滤 / nil 接收者安全 / panic 不吞）；伴随完成 T1.1 trace 早期切片：`internal/trace/{trace,doc}.go` 完整 16 个 Type 常量 + 4 级 Level + Event 结构 + Recorder 接口 + WithTrace/FromContext ctx 帮助 + NopRecorder；uio.Sink.EmitTrace 强依赖 trace.Event 因此同步落地 | 14 测试全绿（trace 5 + uio 9：含 ctx 中断优先 / 多路分发顺序 / panic 传播 / 枚举顺序）；`make vet`/`make test` 全过；go.mod 0 新增依赖 |
| 2026-05-25 | 开发 | **T1.3 + T1.4 完成**：`internal/llm/network/{errors,retry,timeout}.go` 共享网络层（HTTPError + 泛型 WithRetry[T] 指数退避 + jitter + Retry-After 优先 + 双层超时 + shouldRetry 5xx/429/408/net.Error）；`internal/llm/tokenest/charratio.go` Estimator + 中英文加权近似（zh=0.6/en=0.25）；`internal/llm/registry.go` Registry 接口 + memRegistry（按 prefix/Capabilities.Model 双路 lookup + 模糊引用 + SplitModelRef）；`internal/llm/openai/` 7 文件 OpenAI 兼容 Provider 完整实化（Config + 6 模型 modelTable + ComputeCost + mapThinkingEffort + 双向 codec + streamConsumer 含 reasoning_content 旁路提取 + Provider 完整 5 方法 + network.WithRetry/WithTimeout 包装）；锁 `openai-go v1.12.0` | 56 个新测试全绿（network 14 + registry 13 + tokenest 7 + openai 22）；`make vet`/`make test` 全过 |
| 2026-05-25 | 开发 | **T1.6 完成**（按 R7-1' 模板）：`internal/tool/{tool,errors,registry,doc}.go` 包基础（Tool 接口 + Result 4 字段含 UserLimited/ForcedTruncated + 9 ErrorCode + 5 Category + 4 Mode + memRegistry 启动期 D84 校验 + Plan 模式过滤）；`internal/tool/testkit/suite.go` 共享 testify/suite（4 公共测试 + UPDATE_TOOL_GOLDENS=1 自动写 golden 模式）；`internal/tool/fs/readfile.go` 完整实现：ReadFileArgs typed struct + invopop/jsonschema reflect + 严格 DisallowUnknownFields + 二进制 NUL 探测 + 6 位行号 + 字节预算截断 + `<file>` 标签包装 + 警告内嵌；`testdata/read_file.schema.golden.json` golden；Makefile `update-tool-goldens` 修复（`-run /TestSchemaGolden` 匹配 testify 子测试）；锁 `invopop/jsonschema v0.14.0` + `stretchr/testify v1.11.1` | 25 测试全绿（tool 9 + readfile 16，含 testkit 公共 4 + 11 read_file 专属用例覆盖 §10.3.8 全部场景）；`make update-tool-goldens` 验证可写 golden 且与现版本一致 |
| 2026-05-26 | 开发 | **T1.8 完成（v1）+ T1.9 阻塞标记**：`internal/bootstrap/{bootstrap,doc}.go` 完整 v1：`App` 结构体（Config/Logger/Trace/Repository/LLM/Tools/Sink/Prompter + LIFO closers 栈）+ `BootstrapV1(cfg) (*App, error)` 6 步装配（slog → trace.Nop → openRepository（mkdir + OpenAndMigrate + store.New 包装 *sql.DB）→ buildLLMRegistry（遍历 OpenAICompat[] → openai.Config + 网络层投影 + Validate + openai.New + Registry.Register；空数组 fail；ActiveModel 失败降级到第一注册）→ buildToolRegistry（注册 read_file + cwd callback）→ NopSink/NopPrompter）；`expandUserPath` 处理 `~/`/`$HOME/`；`Close()` 幂等 + nil 安全；失败路径自动 cleanup；`internal/cli/cmd/root.go` placeholder runREPL 接入 BootstrapV1 输出装配诊断（database/providers/tools/active 4 行）后仍按 placeholder 契约非零退出。**T1.9 端到端冒烟标 ⛔ 阻塞**：依赖 T2.6 agent.Loop + T2.1/T2.2 permission gating + T1.7 cli/repl 实化，单靠 BootstrapV1 无法跑通"读文件并总结"用例 | 12 测试全绿（expandUserPath 5 子用例 / Happy path / 无 provider 失败 / 缺 APIKey 失败 / nil cfg 失败 / Close 幂等 / nil App.Close 安全 / 自动创建多级父目录）；`make vet`/`make test` 全过 |
| 2026-05-26 | 开发 | **T2.2 完成 + T2.1 标 ✅（T1.6 期已落）**：`internal/permission/` 7 文件完整实化（R5 §5.5 + R4 §7.3 + §7.4）；Mode 用 `type Mode = tool.Mode` 别名解决 permission↔tool 循环依赖；硬黑名单 starter set 15 条（rm -rf / fork bomb / 系统文件 / 路径级写）；matchHardDeny 双 pass 设计（patternHasGlobMeta 判定 → 字面相等 OR doublestar，前者解决 fork bomb 含 `{` 被 doublestar 误判 alternation 的 bug）；normaliseSpaces 折叠多余空白；Substitutor 处理 `${cwd}`/`${home}`/`~` 含未配置时 fail-closed；matrixDecision 完整实化 §4.3 5×5 矩阵；gate 5 步流程 + sync.RWMutex + RememberApproval (path>command>sha256(args)) + 非零 ctx 即拒 + nil prompter fail-closed；LoadRules 文件不存在 fail-soft；锁 `bmatcuk/doublestar/v4 v4.10.0`；同步增强 bootstrap App 加 Permission 字段 + step 5.5 buildGate + mapPermissionMode 投影 + root REPL 诊断加 mode 行 | 49 测试全绿（Substitutor 8 / matchUserRule 4 / matchHardDeny 4 / matrixDecision 16 / CheckRulesOnly 5 / Check 6 / SetMode 1 / LoadRules 4 / Enum 1 / approvalKey 4 / NewGate guard 1 + bootstrap 加 8 = 总 28 个测试）；`make vet`/`make test` 13 包全过 |
| 2026-05-26 | 开发 | **T2.3 完成**（write_file / edit_file / delete_file，按 R7-1' §10.7 模板填空）：`internal/tool/fs/{writefile,editfile,deletefile}.go` + 各自 golden + 测试；**write_file** 1 MB 硬上限 + `*bool` mkdir_parents 三态 + 原子写（CreateTemp+Write+Sync+chmod 0644+Rename，失败 best-effort cleanup `.mini-agent-write-*.tmp`）；**edit_file** 2 MB 硬上限 + `*int` expected_occurrences 三态 + strings.Count 严格计数 + 不一致返回 `ErrAmbiguous` 含前 3 处 1-based 行号（`findOccurrenceLines` 单遍 `bytes.Index`+`bytes.Count`）+ 0 处 `ErrNotFound` + 拒绝 directory / old==new / expected≤0；**delete_file** 单字段 + 拒绝 directory / 非 regular 文件 / symlink 仅删 link；read_file 的 `resolvePath` 方法重构为调包级 `resolvePath` 函数让所有 fs 工具复用；bootstrap 升级按字母序注册 4 个工具，test 改用严格 roster 断言；新增 `make update-tool-goldens` 跑通生成 3 个新 golden 文件 | 53 个新测试（write 14 / edit 22 含 findOccurrenceLines 8 子用例 / delete 9 / bootstrap 改 8）；fs 包累计 69/69 ✅；`make vet`/`make test` 13 包全过 |
| 2026-05-26 | 开发 | **T2.3 扩展完成（只读工具补全）**：dev-plan 未单列但 §10.7 P0 ReadOnly 表必须落地；新增 `internal/tool/fs/listdir.go` + `internal/tool/search/{grep,glob,doc}.go` 三个工具按 R7-1' 模板。**list_dir** 默认 12 条 ignore set + 用户传入则完全替换 + 总条数硬上限 5000 + 双 pass `matchIgnore`（路径含 `/` doublestar full / 否则 basename + 中段 segment）；**grep** RE2 + 三 output_mode（content/files_with_matches/count）+ context_before/after merge + multiline=true 自动 `(?s)` + 文件级硬上限 10MB（skipped+warning）+ 总匹配上限 1000 + 默认 ignore set 与 list_dir 一致；**glob** doublestar v4 + ValidatePattern + GlobWalk + `splitAbs` 处理绝对 pattern + 总数 5000 + **故意不应用默认 ignore**；search 包内本地复制 `resolvePath`/`schemaToMap`/`matchIgnore`（避免 fs↔search 互依赖）；bootstrap 升级注册 7 工具按字母序 + bootstrap_test roster 断言更新 | 53 个新测试（listdir 13 含 testkit 4 + 9 专属 + matchIgnore 10 / search 40：grep 14 + glob 12 + splitAbs 4）；fs 包累计 82 测试 / search 包 40 测试 / 14 包全过 |
| 2026-05-26 | 开发 | **T2.4 完成（bash 工具 + 硬黑名单细化）**：`internal/tool/shell/{bash,doc}.go` + `bash_test.go` + golden；走 `/bin/bash -c <command>` 让 shell 自己处理复合命令（不自实现 shellwords）；进程组级 SIGKILL（Setpgid+`kill -pgid`）防止 children 脱离；50KB cappedBuffer for stdout/stderr；ctx 取消/timeout 区分 ErrInterrupted/ErrTimeout；非零 exit 返回 Result 含 exit_code 不报错；env_overrides 空值=删除；timeout clampInt [1,300]；启动期 Windows 短路（POSIX shell 不可用）。**硬黑名单 starter 15 → 50+ 条**：rm 6 变体 / 块设备 10 / fork bomb 2 / 系统文件 redirect+tee 14 / pipe-to-shell 6 / chmod-chown 6 / git push --force 5 / 路径级 7（新增 /etc/hosts、/boot/**、/root/.ssh/**）。**关键架构修复**：matchHardDeny 的 bash 命令匹配从 doublestar 改为自定义 `matchBashPattern`（编译为 anchored regexp，`*`/`**` 等价 = `.*`，其余 metachar 严格 escape）——doublestar 的 `*` 不跨 `/` 对命令字符串语义错位（典型反例 `dd if=/dev/zero of=/dev/sda` 与 `curl https://x \| sh`）；锁 `regexp` 进 permission 包；bootstrap 注册 8 工具按字母序 + 同步更新 roster 断言；修一个测试 `TestCheckRulesOnly_UserDenyBeforeAllow` 用 feature 分支命令绕开新增的 main/master 黑名单 | bash 30 测试 + permission 黑名单 49 新测试（bash table 31 + benign 7 + path 11 + sanity 1）；累计 permission 包 98 测试；`make vet`/`make test` 15 包全过 |
| 2026-05-26 | 开发 | **T2.5 完成（ask_user 工具，P0 工具集收尾）**：`internal/tool/ask/{askuser,doc}.go` + `askuser_test.go` + golden；AskUserArgs 二字段（question 必填 / hint 可选）；构造期注入 `uio.Prompter` 而非 callback（与 R3 uio 设计对称，未来能复用 Sink 等其它能力）；Category=Meta 让 matrixDecision 在所有模式包括 yes/plan 放行（D86）；输出 `<ask_user><question>...</question><answer>...</answer></ask_user>` 含 XML escape；nil prompter fail-closed → ErrInvalidArgs；prompter error 优先映射 ctx 否则 ErrIO；snippet trace 60 字符截断；bootstrap 流程重排：NopPrompter/NopSink 装配从 step 6 提前到 step 4.5（让 step 5 的 buildToolRegistry 能拿到非 nil prompter），`buildToolRegistry(cfg, prompter)` 签名扩展；按字母序注册 9 工具收尾 | 17 个 ask_user 测试（testkit 4 + 行为 9 + pure-fn 4）；`make vet`/`make test` 16 包全过 | 至此 P0 工具集 9 个完整就绪，可进入 T2.6 agent.Loop |
| 2026-05-27 | 开发 | **T2.6 完成（agent.Loop v1）**：`internal/agent/{agent,signature,failure_counter,prompts,deps,interrupt,exec,loop,doc}.go` + `prompts/system.md`（go:embed）；类型族（Runner/RunInput/RunResult/StopReason 4 态/Config + CompactionConfig/Deps 8 字段/MissingDepError）；signature.go 实化 D56 `<tool>:sha256[:8]hex` + canonicalJSON 递归排序 map keys + 不规范化路径让换写法重置计数；failure_counter.go D57 sync.Map+atomic.Int64；prompts/system.md ~110 词 D49–D51 + text/template 渲染 cwd/OS/RFC3339；BuildSkillList D54 第 2 槽 + BuildAgentsMD D52 `<project_guidelines>` 包裹；deps.go 三占位接口（Compactor/SkillLoader/AgentsMDLoader）+ Nop 实现；interrupt.go synthesizeInterruptedResult/toolResultMsg/extractToolUseBlocks；**exec.go D58/D59 双桶并行**：readonly sync.WaitGroup 全跑（不取消兄弟）+ sequential 串行 + ctx 取消时填合成 interrupt 占位；executeToolCall 五步（lookup → Gate.Check → Sink.EmitToolCallStart → Invoke → Sink.EmitToolCallEnd），DenyHard/Deny 不计入失败计数器（D66），≥ToolRetryMax 时附 D76 "please try a different way" 提示；**loop.go Run 主循环**：prepareInitialHistory 3 独立 system 消息（D54）+ Resume 持久化历史 + UserMessage；for step{ ctx 检查 → MaybeCompact → streamOnce → 持久化 assistant → 0 tool_use 即 StopEndTurn → executeToolCalls → 持久化 tool_results → ctx 取消即 StopInterrupted }；nil Sink/Prompter 自动兜底；getCwdFromEnv 走 os.Getwd（T3 改 session.Cwd）；**bootstrap step 6 装配 agent.Loop**：App.Agent 字段 + agent.New 必填校验 + Compactor/SkillLoader/AgentsMDLoader 留 nil 走 Nop + cfg.Agent 投影 MaxSteps/ToolRetryMax | 32 个 agent 测试（signature 6 / failureCounter 5 / prompts 6 / interrupt 4 / loop e2e 9：EndTurnWithoutTools / OneToolCallThenEndTurn / FailingToolHintAfterRetryMax / MaxStepsCap / CtxCancelMidLoop / ProviderStreamError / ParallelReadOnlyBucket / NilSinkPrompter / NewRejectsMissingDeps × 4 子用例）+ bootstrap Agent 字段断言；17 个包 `make vet`/`make test` 全过；用 fakeProvider + memRepo + echoTool/failTool + 真实 permission.NewGate(ModeYes) 端到端验证 |
| 2026-05-27 | 开发 | **T2.8 完成（AGENTS.md 加载）**：`internal/agentsmd/{loader,doc}.go` + `loader_test.go`；Loader 接口 `Load(ctx, cwd) (string, error)` 与 agent.AgentsMDLoader 完全匹配（无需适配器）；Config 三字段（GlobalPath / ProjectLookup / MaxBytes）+ 运行期 `~/~/foo` 展开；**D27 实化**：全局 → 项目级，不向上递归；**§7.2.2 实化**：`\n\n---\n\n` 拼接，TrimSpace 后空 → 缺失语义；**D53 实化**：>1MB 截断 + `[...truncated, N bytes total]` + `info.Size()` 真实尺寸；**§7.2.5 fail-soft**：文件不存在 / 空 / 读失败 / 目录 / 权限拒绝全部静默返空。bootstrap step 5.6 实化注入；prepareInitialHistory 已支持，无需改 Loop。fakeProvider 加 `lastReq` 字段让集成测试断言 system 消息内容 | 32 个 agentsmd 测试（discovery 6 / emptiness 4 / truncation 2 / construction 3 / ctx 1 / tilde 7 / merge 6 / cwd reload 1 + nil cfg 1 + filesys 1）+ 4 个 agent 集成测试（GlobalAndProject 注入 / Absent 不注入 / Loader error fail-soft / 错误时不漏 wrapper）；agent 包 32→36；18 个包 `make vet`/`make test` 全过 |
| 2026-05-27 | 开发 | **T1.9 完成（端到端冒烟）**：`internal/bootstrap/e2e_smoke_test.go` — 3 轮 ReAct（read_file → list_dir → 文本 summary），**绕过 cli/repl 直接驱动 `app.Agent.Run`**。fixture：BootstrapV1 全装配 + temp cwd（README.md/AGENTS.md/go.mod）+ 真实 SQLite + 真实 9 工具 + 真实 Gate(ModeYes) + 真实 agentsmd + scriptable mockProvider 经 `Registry.Register + SetActive` 注入并重建 Loop；captureSink 捕获事件。**8 大断言全过**：StopReason=end_turn / Steps=3 / mock 调 3 次 / `<project_guidelines>` 三轮都含 / Sink.toolStarts=2+toolEnds=2 顺序正确 / round-2 tool_result 真含 README 内容 / Repo 持久化 6 条 + summary 文本 / Usage=150/60。**关键发现并修复 Loop v1 bug**：UserMessage 之前只入 history 送 LLM 但未持久化，导致 Resume/`/show`/Web 视图丢失用户输入；修复为 `prepareInitialHistory` 之后调 `AppendMessage` 用 `session.FromLLM` 包装；同步更新 3 个旧 loop 测试期望值 | 1 个新 e2e + 修 3 个旧 loop 期望值；18 个包 `make vet`/`make test` 全过；端到端首次跑通 zero-network |
| 2026-05-27 | 开发 | **T2.9 完成（四模式端到端验收）**：`internal/bootstrap/four_modes_e2e_test.go` — 9 个 e2e 用例覆盖 §4.3 工具×权限矩阵代表性单元格（C1 Default+read_file 放行 / C2 Default+write_file 用户拒绝 / C3 AutoEdit+write_file 放行 / C4 Yes+bash 安全 / C5 Yes+bash 硬黑名单短路 / C6 Plan+read_file / C7 Plan+write_file DenyMode / C8 Default+ask_user Meta 放行 / Sanity）。fixture 抽象 `newFourModeFixture` + approve/deny prompter fake。**发现并修复两个 Loop bug**：(Bug-1) Gate 拒绝时 Loop 不发 Sink 事件 — 修复：exec.go 新 `emitDeniedToolEvents` 在 Deny/DenyHard 分支补发 Start+End 对带拒绝原因 Err；(Bug-2) ask_user 装配期固化 NopPrompter，运行期 RunInput.Prompter 用不上 — 修复：新增 `internal/uio/context.go`（WithPrompter/PrompterFromContext + WithSink/SinkFromContext）让 Loop 在 Invoke 前注入 ctx，ask_user 优先取 ctx prompter 回退构造期；同样的桥接支持未来 CLI+Web 并发 Run 共享 tool registry | 9 个 four_modes 测试全过；18 个包 `make vet`/`make test` 全过；§4.3 矩阵端到端验证完毕，至此 Iter-2 仅剩 T2.7（卡 R9）|

---

## 五、阻塞登记（Blocker Log）

> 每次出现阻塞时记录：编号、描述、阻塞起始时间、影响任务、解除条件、解除时间

当前无阻塞。

---

## 六、迭代回顾（Retrospective）

每完成一个 Iter 后在此追加：

### Iter-0 回顾
> ⏳ 未完成

### Iter-1 回顾
> ⏳ 未完成

### Iter-2 回顾
> ⏳ 未完成

### Iter-3 回顾
> ⏳ 未完成

### Iter-4 回顾
> ⏳ 未完成

### Iter-5 回顾
> ⏳ 未完成

### Iter-6 回顾
> ⏳ 未完成

---

## 七、下一步行动

> 每次更新本文件时，明确写出"下一步要做什么"

**当前下一步**：**Iter-2 收尾仅剩 T2.7（REPL 斜杠命令派发）一项，卡在 R9 设计轮次**。R9 需要锁定的协议：`/cd <path>` / `/model <name>` / `/show-system` / `/show-archived` / `/show-hidden` / `/clear` / `/compact` / `/perm` / `/exit` 等命令的输入解析、消息持久化语义（user 输入 `/cd` 是否产生 user 消息？）、与 Sink 的交互（如何打印命令结果）、错误恢复（无效命令是 user 消息还是直接报错）。在 R9 锁定前，**Iter-2 已经端到端可跑通**：开发者可以通过写脚本调用 `app.Agent.Run` 完成所有 P0 任务（这正是 T1.9 / T2.9 已验证的）。

**继续推进的两个候选方向**：
1. **Iter-3 启动**（T3.x：压缩 / 子 agent / 流式渲染细化）—— Iter-2 的 P0 闭环已就绪，可直接进入 P1 功能
2. **R9 设计**（解锁 T2.7 / T1.7） —— 需要再开一轮系统设计讨论

**Iter-0 剩余 1 项**：T0.5 日志封装（继续等 R12）。**Iter-1 已完成 7/9**（T1.7 卡 R9）。**Iter-2 已完成 7/9 + 只读工具补全子任务**（T2.1 + T2.2 + T2.3 + T2.3 扩展 + T2.4 + T2.5 + T2.6 + T2.8 + T2.9 + T1.9）。**P0 工具集 9 + agent.Loop v1 + AGENTS.md + 端到端冒烟 + 四模式验收全通**。

**已就绪可立即开发的任务**（按 Iter 顺序）：
- **Iter-0**：（无；T0.5 等 R12）
- **Iter-1**：（无新可立即开发；T1.7 等 R9；T1.9 阻塞依赖 T2.6）
- **Iter-2**：（仅剩 T2.7 卡 R9）
- **Iter-2**：T2.1 tool.Registry / T2.2 permission.Gate / T2.3 write_file/edit_file/delete_file（按模板）/ T2.4 bash（含硬黑名单实现期补充）/ T2.5 ask_user / T2.6 agent.Loop / T2.8 AGENTS.md 加载
- **Iter-4**：T4.5 子 agent 继承 skill / T4.9 Anthropic 适配 / T4.10 白黑名单
- **Iter-5**：T5.4 serve 子命令

**待后续设计回填的任务**：
- T0.5 日志封装（依赖 R12）
- T1.1 trace 事件类型（依赖 R12）
- T1.7 / T2.7 / T4.4 / T4.8 REPL 与斜杠命令（依赖 R9）
- T3.1 / T3.2 压缩 + /compact（依赖 R8）
- T3.3 write_plan / T3.4 task / T4.3 skill_tool（依赖 R7-2 P1 工具 schema）
- T4.7 详细 Trace 落地（依赖 R12）
- T5.1–T5.3 / T5.5 / T5.6 / T6.* Web UI（依赖 R10/R11）
- T7.1 MCP Client（依赖 R13）

**建议开发顺序**：T0.1 ✅ → T0.2 ✅ → T0.3 ✅ → T0.6 ✅ → T0.4 ✅ → T0.7 ✅ → T1.5 ✅ → T1.2 ✅ → T1.3 ✅ → T1.4 ✅ → T1.6 ✅ → T1.8 ✅ → T2.1 ✅ → T2.2 ✅ → T2.3 ✅ → T2.3+ ✅ → T2.4 ✅ → T2.5 ✅ → T2.6 ✅ → T2.8 ✅ → T1.9 ✅ → T2.9 ✅ → **下一步 T2.7（REPL 斜杠命令派发，等 R9）or 启动 Iter-3 推进 P1**。
