// Package skill hosts the skill_tool — loads a specific SKILL.md into
// the assistant context on demand. Loaded content carries
// UserVisibility=hidden (D24) so the UI shows only a short summary.
//
// Status: skeleton only. Implementation tracked by T4.3 (depends on
// R7-2 schema lock-in).
package skill
